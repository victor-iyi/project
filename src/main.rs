fn main() {
  // TODO: Create a `.lotlinx.toml` configuration file.
  println!("Hello, World!");
  // $ lotlinx --version
  // $ lotlinx --help
  //
  //  `new` -- subcommand.
  // $ lotlinx new pricing -- Creates a new TF estimator project template.
  // $ lotlinx new pricing --engine keras -- Creates a new keras project template.
  // $ lotlinx new pricing --engine keras --py_version 3.7 --runtime 2.1
  // -- Creates a new keras project template with a python version of 3.7, GCS runtime version of 2.1,
}
